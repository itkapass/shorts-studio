"""engine.character — original vector cast + lip sync for the character_skit style."""
from .library import (
    character_defaults,
    available_characters, character_labels, build, cast_for_archetype,
    CHARACTERS, CAST_BY_ARCHETYPE, VISEME_ORDER,
)
